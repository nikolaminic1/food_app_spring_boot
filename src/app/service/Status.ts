export enum Status {
  LOADING,
  REJECTED,
  SUCCEED,
  IDLE,
}

export enum NotificationType {
  WARN,
  SUCCESS,
  ERROR,
  INFO,
}

export enum ValidateStatus {
  ERROR = "error",
  SUCCESS = "success",
  WARNING = "warning",
  VALIDATING = "validating",
}
