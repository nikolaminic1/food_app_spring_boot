import React, { FC, ReactElement, useState, useEffect } from "react";

interface Props {
  id: number;
}

const RestaurantOrderSection: FC<Props> = ({}): ReactElement => {
  return <div></div>;
};

export default RestaurantOrderSection;
